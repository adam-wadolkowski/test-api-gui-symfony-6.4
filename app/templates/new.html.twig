{% extends 'base.html.twig' %}

{% block title %}New post on blog{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Add new post</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('list_blog_posts') }}">
                    View All Post on Blog
                </a>
            </div>
            <div class="card-body">
                {{ include('_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}